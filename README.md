# MPRemoteCommandCenterSample
Play streaming audio with appropriate UI in Control Center and lock screen

This sample plays an audio stream using `AVPlayer()`.  Background audio is enabled.

It uses `MPNowPlayingInfoCenter` and `MpRemoteCommandCenter` to display an appropriate UI in the 
Control Center and on the lock screen.
